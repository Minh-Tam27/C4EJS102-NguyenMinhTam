console.log('Ex3:');
console.log('3.1.	A variable declared by let, in a function still exists after the function finishes running if still in a scope');
console.log('3.2.	A variable declared by var, in a function still exists after the function finishes running');